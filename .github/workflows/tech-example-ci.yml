name: Test appointment API example

on:
  push:
    paths:
      - "examples/appointment-api/**"
      - ".github/workflows/tech-example-ci.yml"
  pull_request:
    paths:
      - "examples/appointment-api/**"
      - ".github/workflows/tech-example-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: python -m pip install -r examples/appointment-api/requirements.txt
      - name: Run isolated API tests
        working-directory: examples/appointment-api
        run: python -m pytest -q
